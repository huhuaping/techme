---
title: "畜禽标准化示范"
author: "胡华平"
date: "`r Sys.Date()`"
output:
  bookdown::word_document2:
    fig_caption: yes
    toc: yes
    toc_depth: 4
    reference_docx: report-reference.docx
  bookdown::html_document2:
    number_sections: yes
    toc: no
    fig_caption: yes
    toc_float: yes
always_allow_html: yes
---

```{r, echo=FALSE}
knitr::opts_chunk$set(echo=FALSE, warning=FALSE, message=FALSE, eval = FALSE,
                      fig.align='center',fig.width=10, fig.height=7) # Places figures on their own pages
options(
  htmltools.dir.version = FALSE, 
  formatR.indent = 2, width = 55, 
  digits = 2,scipen=999,tinytex.verbose = TRUE,
  knitr.kable.NA = '',
  fig.width=12, fig.height=8)

```

```{r}
require("rvest")
require("xml2")
require("httr")
require("stringr")
require("tidyverse")
require("tidyselect")
require("here")
require("openxlsx")
require(glue)
```

\newpage

# 1.研究目标

畜禽标准化示范场

# 2.资料来源

## 2.1 数据来源


农业部网站

- 【附名单 word】2020年农业农村部畜禽养殖标准化示范场名单[公示](http://www.moa.gov.cn/govpublic/xmsyj/202012/t20201223_6358630.htm)

- 农业农村部办公厅关于公布2019年畜禽养殖标准化示范场名单的[通知](http://www.moa.gov.cn/nybgb/2019/201912/202004/t20200410_6341206.htm)

- 【附名单 Excel】农业农村部2018年畜禽养殖标准化示范场名单[公示](http://www.moa.gov.cn/xw/zxfb/201812/t20181212_6164807.htm)

- 关于公布2017年畜禽养殖标准化示范场名单的[通知](http://www.moa.gov.cn/gk/tzgg_1/tfw/201712/t20171227_6126063.htm)

- 关于公布2016年畜禽养殖标准化示范场名单的[通知](http://www.moa.gov.cn/gk/tzgg_1/tfw/201612/t20161209_5402699.htm)

- 关于公布2015年畜禽标准化示范场名单的[通知](http://www.moa.gov.cn/nybgb/2015/shierqi/201712/t20171219_6104136.htm)

- 关于公布2014年畜禽标准化示范场的[通知](http://www.moa.gov.cn/nybgb/2014/shier/201712/t20171219_6111624.htm)

- 农业部办公厅关于公布2013年畜禽标准化示范场名单的[通知](http://www.moa.gov.cn/nybgb/2013/qsyq/201805/t20180509_6141669.htm)

- 【附名单 word】农业部办公厅关于公布2011年第一批畜禽标准化示范场名单的[通知](https://www.moa.gov.cn/gk/zcjd/201110/t20111018_2363998.htm)

- 【附名单 word】农业部办公厅2010年关于公布第二批畜禽标准化示范场名单的[通知](http://www.moa.gov.cn/govpublic/XMYS/201012/t20101214_1790209.htm)




## 2.2 数据进度

China Agricultural Research System，CARS

现代农业产业技术体系[管理平台](http://123.127.160.231/)

- 【附名单pdf】关于公示现代农业产业技术体系首席科学家和岗位科学家候选人名单的[通知](http://www.moa.gov.cn/govpublic/KJJYS/202108/t20210806_6373623.htm) 另[网页](https://www.waizi.org.cn/law/119114.html)

    - pdf[附件](http://www.moa.gov.cn/govpublic/KJJYS/202108/P020210806358525696440.pdf)


2007年，原农业部、财政部启动建设国家现代农业产业技术体系（以下简称“产业体系”），聚焦水稻、玉米、小麦、大豆、生猪、奶牛等50个主要农产品组建了50个相应的体系，集合了全国800多个科教和企事业单位的2600多名专家进行科技攻关，形成跨部门、跨区域、跨单位、跨学科的优势科技力量，联合协作解决产业重大问题。

在产业体系中，每个岗位科学家每年有70万元、每个试验站站长每年有50万元基本研发费保障。

技术体系是由原农业部、财政部2007年启动建设的一个创新型科研组织。通过聚焦水稻、小麦、生猪等54个主要农产品，该体系组织汇聚了全国800多个科教和企事业单位的2600多名专家进行攻关，形成跨部门、跨区域、跨单位、跨学科的优势科技力量，联合协作解决产业重大问题。

- 2019-07-16 关于公示现代农业产业技术体系2019年增补岗位科学家候选人名单的[通知](http://www.kjs.moa.gov.cn/gzdt/201907/t20190716_6320991.htm)

- [word名单]2017-03-30 关于公示现代农业产业技术体系“十三五”新增岗位科学家候选人名单的[通知](http://www.moa.gov.cn/gk/tzgg_1/tz/201703/t20170320_5530342.htm)


- 【附名单html】（2011-2015）2011-04-02农业部关于印发现代农业产业技术体系建设依托单位和岗位聘用人员名单（2011-2015年）的[通知](http://www.stee.agri.cn/cyjs/201103/t20110313_3136130.htm)
    
    - 2011-04-02现代农业产业技术体系建设依托单位和岗位聘用人员名单（2011-2015年）[通知](http://jiuban.moa.gov.cn/zwllm/rsxx/201104/t20110402_1961261.htm)
    - 2011-04-02农业部办公厅关于印发现代农业产业技术体系执行专家组人员组成名单（2011-2015年）的[通知](http://www.stee.agri.cn/cyjs/201103/t20110313_3136129.htm)

- 【word 名单】（2008-2010）2008-12-22（大麦等40个主要农产品）农业部关于印发现代农业产业技术体系第二批建设依托单位和岗位聘用人员名单的[通知](http://www.stee.agri.cn/cyjs/200903/t20090317_3136148.htm)

- 【word 名单】（2008-2010）2008-12-20（增补）农业部关于印发现代农业产业技术体系第一批试点增补建设依托单位和岗位聘用人员名单的[通知](http://www.stee.agri.cn/cyjs/200903/t20090317_3136144.htm)

- 【word名单】（2007-2010）2007年12月29日 现代农业产业技术体系建设第一批试点建设依托单位和岗位聘用人员[名单](http://www.stee.agri.cn/cyjs/200903/t20090317_3136145.htm)  
    
    - 另一链接[html名单](http://jiuban.moa.gov.cn/fwllm/zxbs/xzxk/ggb/201006/t20100606_1533919.htm) 

- 2007年12月12日 现代农业产业技术体系建设实施方案（试行）[农业部科技教育司](http://jiuban.moa.gov.cn/fwllm/zxbs/xzxk/ggb/201006/t20100606_1533918.htm)


- 2019-11-15 破解科研生产“两张皮”难题的成功探索——国家现代农业产业技术体系建设成效[综述](http://www.kjs.moa.gov.cn/gzdt/201911/t20191115_6331897.htm)

## 异常提示

- 机构名称存在换行符'\n'


# 3.数据抓取流程(data-raw到data-extract)

## 3.1 基本过程

```{r}
require("rvest")
require("stringr")
require("tidyverse")
require("tidyselect")
require("openxlsx")
```


## 3.2 抓取html（全部）

### 2010年名单xlsx

```{r}
#==== files html path====
files_path <- "html/year-2010.xlsx"
Year <- as.numeric(str_extract(files_path, "(\\d{4})"))

num_base <- c("一", "二", "三", "四", "五", "六", "七", "八", "九")  
# the html show max '五十'
num_chn <- c(num_base,"十",
             paste0("十", num_base),"二十",
             paste0("二十", num_base),"三十",
             paste0("三十", num_base),"四十",
             paste0("四十", num_base),"五十")

pattern_chn <- paste0(paste0(num_chn,"、"), collapse ="|")
ptn_area_num <- glue::glue("({paste0(num_chn,collapse='|')})(?=、)")
ptn_area_name <- glue::glue("(?<={pattern_chn})(.+)(?=（)")
ptn_area_tot <- glue::glue("(?<={pattern_chn}.+（)(.+)(?=）)")
ptn_prod_num <- glue::glue("({paste0(num_chn,collapse='|')})(?=）)")

# ====string extract ====
tbl_raw <- openxlsx::read.xlsx(files_path, colNames = F) %>%
  rename(value = "X1") %>%
  filter(!is.na(value)) %>%
  mutate(value = str_trim(value)) %>%
  # extract area
  mutate(area_num = str_extract(value, ptn_area_num),
         area_name = str_extract(value, ptn_area_name),
         area_tot = ifelse(!is.na(area_num),
                           str_extract(value,
                                       "(?<=（)(.+)(?=个）)"),
                           NA)) %>%
  # extract product
  mutate(prod_num = str_extract(value, ptn_prod_num),
         prod_name = ifelse(!is.na(prod_num), 
                            str_extract(value,
                                        "(?<=）)(.+)(?=（)"),
                            NA),
         prod_tot = ifelse(!is.na(prod_num), 
                            str_extract(value,
                                        "(\\d{1,3})(?=个）)"),
                            NA)) %>%
  # fill and filter 
  fill(area_num:prod_tot, .direction = "down") %>%
  filter(!is.na(prod_num)) %>%
  # extract company
  mutate(com_num = str_extract(value, "(^\\d{1,3})"),
         com_name = ifelse(!is.na(com_num),
                           str_trim(str_extract(value,
                                       "(?<= )(.+)")),
                           NA)) %>%
  filter(!is.na(com_num)) %>%
  # column type 
  mutate_at(vars(c("area_tot", "prod_tot")), as.numeric) 

# ====just for check====
str(tbl_raw)
(check <- tbl_raw %>%
  select(area_name, area_tot) %>%
  unique() %>%
  mutate(area_tot= as.numeric(area_tot)) %>%
  summarise(n = sum(area_tot)))

if(nrow(tbl_raw) != check$n) stop("rows not equal to sum tot, please check!")

vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")

# ==== tidy and select====
tbl_tidy <- tbl_raw %>%
  select(-value) %>%
  add_column(index = 1:nrow(.), .before = "area_num")%>%
  add_column(year = Year, .before = "index") %>%
  select(all_of(vars_sel))

# ====write out====
path_out <- glue("xlsx/year-{Year}.xlsx")
openxlsx::write.xlsx(tbl_tidy, path_out)
```

### 2011年名单xlsx

```{r}
#==== files html path====
files_path <- "html/year-2011.xlsx"
Year <- as.numeric(str_extract(files_path, "(\\d{4})"))

num_base <- c("一", "二", "三", "四", "五", "六", "七", "八", "九")  
# the html show max '五十'
num_chn <- c(num_base,"十",
             paste0("十", num_base),"二十",
             paste0("二十", num_base),"三十",
             paste0("三十", num_base),"四十",
             paste0("四十", num_base),"五十")

pattern_chn <- paste0(paste0(num_chn,"、"), collapse ="|")
ptn_area_num <- glue::glue("({paste0(num_chn,collapse='|')})(?=、)")
ptn_area_name <- glue::glue("(?<={pattern_chn})(.+)(?=（)")
ptn_area_tot <- glue::glue("(?<={pattern_chn}.+（)(.+)(?=）)")
ptn_prod_num <- glue::glue("({paste0(num_chn,collapse='|')})(?=）)")

# ====string extract ====
tbl_raw <- openxlsx::read.xlsx(files_path, colNames = F) %>%
  rename(value = "X1") %>%
  filter(!is.na(value)) %>%
  mutate(value = str_trim(value)) %>%
  # extract area
  mutate(area_num = str_extract(value, ptn_area_num),
         area_name = ifelse(!is.na(area_num),
                            str_extract(value, "(?<=、)(.+)"),
                            NA)) %>%
  # extract product
  mutate(prod_com = ifelse(is.na(area_num),value,NA)) %>%
  # fill and filter 
  fill(area_num,area_name, .direction = "down") %>%
  filter(!is.na(prod_com)) %>%
  mutate(prod_com = str_squish(prod_com)) %>%
  # extract company
  separate(col = prod_com, 
           into = c("num_mix", "prod_name", "com_name"),
           sep = " ") 

# ==== tidy and select====
vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")
tbl_tidy <- tbl_raw %>%
  select(-value) %>%
  add_column(index = 1:nrow(.), .before = "area_num")%>%
  add_column(year = Year, .before = "index") %>%
  select(all_of(vars_sel))

# ====write out====
path_out <- glue("xlsx/year-{Year}.xlsx")
openxlsx::write.xlsx(tbl_tidy, path_out)
```

### 2013年名单html

```{r}
# files html path
files_path <- "html/year-2013.html"

Year <- as.numeric(str_extract(files_path, "(\\d{4})"))

# the html show max '五十'
num_base <- c("一", "二", "三", "四", "五", "六", "七", "八", "九")
num_chn <- c(num_base,"十",
             paste0("十", num_base),"二十",
             paste0("二十", num_base),"三十",
             paste0("三十", num_base),"四十",
             paste0("四十", num_base),"五十")

pattern_chn <- paste0(paste0(num_chn,"、"), collapse ="|")
ptn_area_num <- glue::glue("({paste0(num_chn,collapse='|')})(?=、)")
ptn_area_name <- glue::glue("(?<={pattern_chn})(.+)(?=（)")

# xpath for data table
css_tbl <-"body > div.main.bjjM.hd_wzxq > div.border.pd_rl_24.hd_zbftMPContent > div.arc_body.mg_auto.w_855.pd_b_35 > div.TRS_Editor > div > p:nth-child(n+8)"

tbl_raw <- read_html(files_path,encoding = "UTF-8") %>%
  html_nodes(css = css_tbl) %>%
  html_text() %>%
  as_tibble() %>%
  filter(!is.na(value)) %>%
  mutate(value = str_trim(value, "both")) %>%
  # extract area
  mutate(area_num = str_extract(value, ptn_area_num),
         area_name = ifelse(!is.na(area_num),
                            str_extract(value, "(?<=、)(.+)"),
                            NA)) %>%
  # extract product
  mutate(prod_com = ifelse(is.na(area_num),value,NA)) %>%
  # fill and filter 
  fill(area_num,area_name, .direction = "down") %>%
  filter(!is.na(prod_com)) %>%
  mutate(prod_com = str_squish(prod_com)) %>%
  # extract company
  mutate(prod_com = str_replace(prod_com, "．", " ")) %>%
  separate(col = prod_com, 
           into = c("num_mix", "prod_name", "com_name"),
           sep = " ") 

# ==== tidy and select====
vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")
tbl_tidy <- tbl_raw %>%
  select(-value) %>%
  add_column(index = 1:nrow(.), .before = "area_num")%>%
  add_column(year = Year, .before = "index") %>%
  select(all_of(vars_sel))

# ====write out====
path_out <- glue("xlsx/year-{Year}.xlsx")
openxlsx::write.xlsx(tbl_tidy, path_out)

```

### 2014年名单html

```{r}
# files html path
files_path <- "html/year-2014.html"

Year <- as.numeric(str_extract(files_path, "(\\d{4})"))

# the html show max '五十'
num_base <- c("一", "二", "三", "四", "五", "六", "七", "八", "九")
num_chn <- c(num_base,"十",
             paste0("十", num_base),"二十",
             paste0("二十", num_base),"三十",
             paste0("三十", num_base),"四十",
             paste0("四十", num_base),"五十")

pattern_chn <- paste0(paste0(num_chn,"、"), collapse ="|")
ptn_area_num <- glue::glue("({paste0(num_chn,collapse='|')})(?=、)")
ptn_area_name <- glue::glue("(?<={pattern_chn})(.+)(?=（)")

# xpath for data table
css_tbl <-"body > div.main.bjjM.hd_wzxq > div.border.pd_rl_24.hd_zbftMPContent > div.arc_body.mg_auto.w_855.pd_b_35 > div.TRS_PreAppend > p:nth-child(n+14)"

tbl_raw <- read_html(files_path,encoding = "UTF-8") %>%
  html_nodes(css = css_tbl) %>%
  html_text() %>%
  as_tibble() %>%
  mutate(value = str_trim(value, "both")) %>%
  mutate(value = str_squish(value)) %>%
  filter(!(value %in% c("", " ",NA))) %>%
  # extract area
  mutate(area_num = str_extract(value, ptn_area_num),
         area_name = ifelse(!is.na(area_num),
                            str_extract(value, "(?<=、)(.+)"),
                            NA)) %>%
  # extract product
  mutate(prod_com = ifelse(is.na(area_num),value,NA)) %>%
  # fill and filter 
  fill(area_num,area_name, .direction = "down") %>%
  filter(!is.na(prod_com)) %>%
  mutate(prod_com = str_squish(prod_com)) %>%
  # extract company
  #mutate(prod_com = str_replace(prod_com, "．", " ")) %>%
  separate(col = prod_com, 
           into = c("num_mix", "prod_name", "com_name"),
           sep = " ") 

# ==== tidy and select====
vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")
tbl_tidy <- tbl_raw %>%
  select(-value) %>%
  add_column(index = 1:nrow(.), .before = "area_num")%>%
  add_column(year = Year, .before = "index") %>%
  select(all_of(vars_sel))

# ====write out====
(path_out <- glue("xlsx/year-{Year}.xlsx"))
openxlsx::write.xlsx(tbl_tidy, path_out)

```

### 2015年名单html

```{r}
# files html path
files_path <- "html/year-2015.html"

Year <- as.numeric(str_extract(files_path, "(\\d{4})"))

# the html show max '五十'
num_base <- c("一", "二", "三", "四", "五", "六", "七", "八", "九")
num_chn <- c(num_base,"十",
             paste0("十", num_base),"二十",
             paste0("二十", num_base),"三十",
             paste0("三十", num_base),"四十",
             paste0("四十", num_base),"五十")

pattern_chn <- paste0(paste0(num_chn,"、"), collapse ="|")
ptn_area_num <- glue::glue("({paste0(num_chn,collapse='|')})(?=、)")
ptn_area_name <- glue::glue("(?<={pattern_chn})(.+)(?=（)")

# xpath for data table
css_tbl <-"body > div.main.bjjM.hd_wzxq > div.border.pd_rl_24.hd_zbftMPContent > div.arc_body.mg_auto.w_855.pd_b_35 > div.TRS_PreAppend > p:nth-child(n+13)"

tbl_raw <- read_html(files_path,encoding = "UTF-8") %>%
  html_nodes(css = css_tbl) %>%
  html_text() %>%
  as_tibble() %>%
  mutate(value = str_trim(value, "both")) %>%
  mutate(value = str_squish(value)) %>%
  filter(!(value %in% c("", " ",NA))) %>%
  # extract area
  mutate(area_num = str_extract(value, ptn_area_num),
         area_name = ifelse(!is.na(area_num),
                            str_extract(value, "(?<=、)(.+)"),
                            NA)) %>%
  # extract product
  mutate(prod_com = ifelse(is.na(area_num),value,NA)) %>%
  # fill and filter 
  fill(area_num,area_name, .direction = "down") %>%
  filter(!is.na(prod_com)) %>%
  mutate(prod_com = str_squish(prod_com)) %>%
  # extract company
  #mutate(prod_com = str_replace(prod_com, "．", " ")) %>%
  separate(col = prod_com, 
           into = c("num_mix", "prod_name", "com_name"),
           sep = " ") 

# ==== tidy and select====
vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")
tbl_tidy <- tbl_raw %>%
  select(-value) %>%
  add_column(index = 1:nrow(.), .before = "area_num")%>%
  add_column(year = Year, .before = "index") %>%
  select(all_of(vars_sel))

# ====write out====
(path_out <- glue("xlsx/year-{Year}.xlsx"))
openxlsx::write.xlsx(tbl_tidy, path_out)

```

### 2016年名单html

```{r}
# files html path
files_path <- "html/year-2016.html"

Year <- as.numeric(str_extract(files_path, "(\\d{4})"))

# the html show max '五十'
num_base <- c("一", "二", "三", "四", "五", "六", "七", "八", "九")
num_chn <- c(num_base,"十",
             paste0("十", num_base),"二十",
             paste0("二十", num_base),"三十",
             paste0("三十", num_base),"四十",
             paste0("四十", num_base),"五十")

pattern_chn <- paste0(paste0(num_chn,"、"), collapse ="|")
ptn_area_num <- glue::glue("({paste0(num_chn,collapse='|')})(?=、)")
ptn_area_name <- glue::glue("(?<={pattern_chn})(.+)(?=（)")

# xpath for data table
css_tbl <-"body > div.main.boxcenter.container_xxgk.clearfix.xxgkmain > div.ggnr > div > div.gsj_htmlcon > div.gsj_htmlcon_bot > div > p:nth-child(n+16)"

tbl_raw <- read_html(files_path,encoding = "UTF-8") %>%
  html_nodes(css = css_tbl) %>%
  html_text() %>%
  as_tibble() %>%
  mutate(value = str_trim(value, "both")) %>%
  mutate(value = str_squish(value)) %>%
  filter(!(value %in% c("", " ",NA))) %>%
  # extract area
  mutate(area_num = str_extract(value, ptn_area_num),
         area_name = ifelse(!is.na(area_num),
                            str_extract(value, "(?<=、)(.+)"),
                            NA)) %>%
  # extract product
  mutate(prod_com = ifelse(is.na(area_num),value,NA)) %>%
  # fill and filter 
  fill(area_num,area_name, .direction = "down") %>%
  filter(!is.na(prod_com)) %>%
  mutate(prod_com = str_squish(prod_com)) %>%
  # extract company
  #mutate(prod_com = str_replace(prod_com, "．", " ")) %>%
  separate(col = prod_com, 
           into = c("num_mix", "prod_name", "com_name"),
           sep = " ") 

# ==== tidy and select====
vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")
tbl_tidy <- tbl_raw %>%
  select(-value) %>%
  add_column(index = 1:nrow(.), .before = "area_num")%>%
  add_column(year = Year, .before = "index") %>%
  select(all_of(vars_sel))

# ====write out====
(path_out <- glue("xlsx/year-{Year}.xlsx"))
openxlsx::write.xlsx(tbl_tidy, path_out)

```

### 2017年名单html

```{r}
# files html path
files_path <- "html/year-2017.html"

Year <- as.numeric(str_extract(files_path, "(\\d{4})"))

# the html show max '五十'
num_base <- c("一", "二", "三", "四", "五", "六", "七", "八", "九")
num_chn <- c(num_base,"十",
             paste0("十", num_base),"二十",
             paste0("二十", num_base),"三十",
             paste0("三十", num_base),"四十",
             paste0("四十", num_base),"五十")

pattern_chn <- paste0(paste0(num_chn,"、"), collapse ="|")
ptn_area_num <- glue::glue("({paste0('^',num_chn,collapse='|')})(?=、)")
ptn_area_name <- glue::glue("(?<={pattern_chn})(.+)(?=（)")

# xpath for data table
css_tbl <-"body > div.main.boxcenter.container_xxgk.clearfix.xxgkmain > div.ggnr > div > div.gsj_htmlcon > div.gsj_htmlcon_bot > div > span > p:nth-child(n+4)"

tbl_raw <- read_html(files_path,encoding = "UTF-8") %>%
  html_nodes(css = css_tbl) %>%
  html_text() %>%
  as_tibble() %>%
  mutate(value = str_trim(value, "both")) %>%
  mutate(value = str_squish(value)) %>%
  filter(!(value %in% c("", " ",NA))) %>%
  # extract area
  mutate(area_num = str_extract(value, ptn_area_num),
         area_name = ifelse(!is.na(area_num),
                            str_extract(value, "(?<=、)(.+)"),
                            NA)) %>%
  # extract product
  mutate(prod_com = ifelse(is.na(area_num),value,NA)) %>%
  # fill and filter 
  fill(area_num,area_name, .direction = "down") %>%
  filter(!is.na(prod_com)) %>%
  mutate(prod_com = str_squish(prod_com)) %>%
  # extract company
  #mutate(prod_com = str_replace(prod_com, "．", " ")) %>%
  separate(col = prod_com, 
           into = c("num_mix", "prod_name", "com_name"),
           sep = " ") 

# ==== tidy and select====
vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")
tbl_tidy <- tbl_raw %>%
  select(-value) %>%
  add_column(index = 1:nrow(.), .before = "area_num")%>%
  add_column(year = Year, .before = "index") %>%
  select(all_of(vars_sel))

# ====write out====
(path_out <- glue::glue("xlsx/year-{Year}.xlsx"))
openxlsx::write.xlsx(tbl_tidy, path_out)

```
### 2018年名单xlsx

```{r}
#==== files html path====
files_path <- "html/year-2018.xlsx"
Year <- as.numeric(str_extract(files_path, "(\\d{4})"))

# ====read xlsx ====
my_header <- c("index", "area_name", "com_num","prod_name", "com_name")
tbl_raw <- openxlsx::read.xlsx(files_path, colNames = T) %>%
  rename_all(.,~my_header) %>%
  # fill and filter 
  fill(area_name, .direction = "down")  %>%
  mutate_all(., str_trim, side = "both")


vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")

# ==== tidy and select====
tbl_tidy <- tbl_raw %>%
  add_column(year = Year, .before = "index") %>%
  mutate(index = 1:nrow(.)) %>%
  select(all_of(vars_sel))

# ====write out====
path_out <- glue("xlsx/year-{Year}.xlsx")
openxlsx::write.xlsx(tbl_tidy, path_out)

```

### 2019年名单html table

```{r}
# ====files html path====
files_path <- "html/year-2019.html"
Year <- as.numeric(str_extract(files_path, "(\\d{4})"))


# ====xpath  table====
xpath_tbl <-"/html/body/div[4]/div[1]/div[2]/div[1]/div/div/table"

my_header <- c("index", "area_name", "com_num","prod_name", "com_name")

tbl_raw <- read_html(files_path,encoding = "UTF-8") %>%
  html_nodes(xpath = xpath_tbl) %>%
  html_table(header = T, fill = T) %>%
  .[[1]] %>%
  as_tibble() %>%
  # rename
  rename_all(.,~my_header) %>%
  mutate_all(., str_trim, side = "both")

vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")

# ==== tidy and select====
tbl_tidy <- tbl_raw %>%
  add_column(year = Year, .before = "index") %>%
  mutate(index = 1:nrow(.)) %>%
  select(all_of(vars_sel))

# ====write out====
(path_out <- glue("xlsx/year-{Year}.xlsx"))
openxlsx::write.xlsx(tbl_tidy, path_out)

```

### 2020年名单xlsx

```{r}
#==== files html path====
files_path <- "html/year-2020.xlsx"
Year <- as.numeric(str_extract(files_path, "(\\d{4})"))


# ====read xlsx ====
my_header <- c("index", "area_name", "prod_name", "com_name")
tbl_raw <- openxlsx::read.xlsx(files_path, colNames = T) %>%
  rename_all(.,~my_header) %>%
  # fill and filter 
  fill(area_name, .direction = "down") 


vars_sel <- c("year", "index", "area_name", "prod_name", "com_name")

# ==== tidy and select====
tbl_tidy <- tbl_raw %>%
  add_column(year = Year, .before = "index") %>%
  mutate(index = 1:nrow(.)) %>%
  select(all_of(vars_sel))

# ====write out====
path_out <- glue("xlsx/year-{Year}.xlsx")
openxlsx::write.xlsx(tbl_tidy, path_out)

```

