# Tidy dataset from unpivot yearbook table

Tidy dataset from unpivot yearbook table

## Usage

``` r
wfl.tidyTable(dt)
```

## Arguments

- dt:

  data.frame. typically the yearbook unpivot table by use `unpivot`.

## Value

data.frame

## Examples

``` r
if (FALSE) { # \dontrun{
df_tidy <- wfl.tidyTable(dt = df_out)
} # }
```
