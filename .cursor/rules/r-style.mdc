---
description: techme 的 R 写法：可读、透明、tidyverse 优先、新代码可用 |>
globs: "**/*.{R,Rmd,qmd}"
alwaysApply: true
---

# R 编程习惯

本项目以 R 为主，脚本多为多年维护的流水线。先读再改，不要另起一套封装。

## 风格

- **tidyverse 优先**：`dplyr` / `stringr` / `tidyr` / `purrr`。
- **管道**：新改的脚本或新写的代码行用原生 `|>`（R ≥ 4.1）。不要整文件只为换管道而改 `%>%`。同一条管道内不要混用 `|>` 与 `%>%`。
- **base R 作辅助**：`stop()`、`file.exists()`、简单下标、`as.numeric()` 即可，不必再包一层。

## 可读（给人类看）

- 短管道；中间对象用现有名字（`df_out`、`df_tidy`、`tbl_dir`）。`select()` 写明列名。
- 重要函数调用、关键参数、每个工作流步骤上方加**一行人话注释**（做什么、为何选这个参数），语气对齐邻近脚本。
- 不要给 `i <- i + 1` 这类自明代码写注释；不要用注释代替可读的对象名。

## 透明（少隐式状态、少复杂度）

- 不要使用过度复杂、多层次的嵌套函数，可以拆分为多步骤的多个可读性函数。
- 函数用到的路径、表、模式都从**参数传入**，不要依赖未声明的全局对象（如入口脚本里的 `file_xlsx`）。
- 不要用 `options()`、`assign()`、`<<-` 传递流水线状态；步骤结果写成中间对象，方便 `View()`。
- 能在入口脚本里写清的三五行，不要再包成新 `wfl.*`。先复用 `R/workflow_funs.R`。
- 参数写全关键项（`header.mode`、`cols.drop`），少靠默认值“碰巧正确”。

## 连续性

- 改年鉴入口时只动顶部设置和当前步骤，后段流程保持不动。
- 新逻辑对齐邻近脚本的注释语气和 `View()` 检查点。
- 修改 `R/` 导出函数后运行 `devtools::document()`。

## 避免

- 为一次性清洗再写深层 helper 或 `purrr::compose` 套娃。
- 用 `suppressWarnings()` 掩盖数据问题；先处理空值/脚注，再转换类型。
