---
description: techme 项目全局约束与 Agent 行为准则
alwaysApply: true
---

# techme 项目核心规则

## 项目定位

- R 数据包 + ETL 流水线，服务于《中国旱区农业技术发展报告》，**非 Web 应用**。
- 首读 [AGENTS.md](../../AGENTS.md) 与 [vignettes/articles/pkg-design.Rmd](../../vignettes/articles/pkg-design.Rmd)。

## 通用原则

- 用中文回复用户。
- **最小改动**：只改任务相关文件，不做无关重构。
- 匹配现有命名、导入风格（`@importFrom`、`%>%` 管道）与 roxygen markdown 风格。

## 禁止手改的文件

- `NAMESPACE` — 由 `devtools::document()` 生成
- `man/*.Rd` — roxygen 自动生成
- `data/*.rda` — 仅经 `wfl.useData()` / `usethis::use_data()` 写入

## 修改 R 函数后

总是提醒或执行：

```r
devtools::document()
```

## 数据安全

- 仓库 private，禁止在对话中粘贴 `data-raw/` 原始 xls/html/抓取内容。
- 不将大体积原始数据纳入 git 提交，除非用户明确要求。

## 交互式脚本

含 `readline()` 的步骤（如 `wfl.writeXlsx`、`wfl_editXls`）标记为**需用户在 RStudio 手动执行**。

## Windows 依赖

- CNKI xls 转换依赖 `excelcnv.exe`（仅 Windows + Office）。
- 不在 Linux/macOS 环境假设该工具可用。

## 工作流 Skills

按任务类型读取 `.cursor/skills/techme-*/SKILL.md`，不要凭记忆跳步。
