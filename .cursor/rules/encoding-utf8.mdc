---
description: 对话中改文件时保持 UTF-8，避免中文乱码（写入约定 + afterFileEdit hook）
alwaysApply: true
---

# 改文件时保持 UTF-8

源文件（`.R` `.Rmd` `.qmd` `.md` `.mdc`）为 **UTF-8 无 BOM**。本规则在每次对话中生效；`afterFileEdit` hook 会在写入后抽查。乱码修复 SOP 见 `techme-encoding-utf8` skill。

## 怎么改（优先）

- 含中文的文件优先 **StrReplace / 局部补丁**，不要整文件重写。
- 直接以 UTF-8 写入。不要按 GBK、cp936、latin-1 解码后再存。
- 需要脚本写文件时：Python `Path.write_bytes(text.encode("utf-8"))`，字面量用 `\uXXXX`。

## 禁止

- `errors="replace"` / `errors="ignore"` 后写回（会变成 `?` 或 U+FFFD）。
- PowerShell `>` / `Out-File` 导出源文件（默认 UTF-16 LE）。
- 整文件改换行（保持该文件原有 CRLF 或 LF）。
- 对已损坏文件「打开并保存」。

## 写完

hook 若注入 UTF-8 失败上下文：立刻按 skill 从 git 历史恢复，不要继续改别的文件。主动自检：`python scripts/check-utf8.py <paths>`。
